*A solvable puzzle began as a solved one
*Unit tests for moving grid (in input_handler): 
	-check that a tile can't be moved to an occupied space
	-check that clicking outside of the grid space throws an error
*Unit test for reversed direction